import { GenerateButtons, GenerateColorButtons, DisplayButtons, DisplayColorButtons } from "./myFunctions.js"
const arrButtons = GenerateButtons();
DisplayButtons(arrButtons);
const arrColorButtons = GenerateColorButtons();
DisplayColorButtons(arrColorButtons);